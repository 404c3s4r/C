#include <stdio.h>

// Implicit Conversion

/* 
In a division between two or more integers, the result is an integer (actually a truncated number). 
This conversion is done automatically by the compiler. 
*/


/*
Example 1:
int main(){
    int n = 10;
    int y = 3;
    int total = n / y; 
    printf("%d", total);
    return 0;
}
*/

int main(){
    float IDfloat = 9;
    printf("%f", IDfloat);
    return 0;
}
