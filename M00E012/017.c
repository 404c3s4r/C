#include <stdio.h>


// ASCII --> https://www.w3schools.com/charsets/ref_html_ascii.asp
main(){
	char a = 36; 
	printf("%c", a);
	return 0; 

}//The output will be a dollar sign.



// In C, we don't use char to handle or store characters; instead, we use arrays of characters or pointers (strings) provided by the standard C library functions. 
//With these functions, we can allocate memory for strings
