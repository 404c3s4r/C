#include <stdio.h>

int main() {
    char c;

    // Lendo um caractere da entrada padrão
    printf("Digite um caractere: ");
    c = getchar();

    // Imprimindo o caractere lido
    printf("O caractere lido foi: %c\n", c);

    return 0;
}
