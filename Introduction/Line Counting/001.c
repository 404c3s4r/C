#include <stdio.h>
#include <string.h> 

/*
int main(){ 
    int c, n1; 
    n1 = 0;  // Adicionei o ponto-e-vírgula que faltava aqui
    while ((c = getchar()) != EOF){
        if(c == '\n')
            ++n1; 
        printf("%d\n", n1);
    }
    return 0;  // Adicionei a declaração de retorno para indicar o final da função main
}
*/ 

/* 
int main() {
    char string[] = "helloworld";
    // int length = strlen(string);
    int lenght = sizeof(string) / sizeof(string[0]);

    printf("A string %d", lenght);

    return 0;
} 
// // Returns 11 characters: 'h', 'e', 'l', 'l', 'o', 'w', 'o', 'r', 'l', 'd'. If we used the function "strlen", we would receive "10 characters".

*/





