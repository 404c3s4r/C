#include <stdio.h>

// A function is a block of code which only runs when it is called.

void myfunctions(){
	//code to be executed, but does not have a return value 
	printf("This code is just got executed, but does not have a return value\n");
}

int main(){
	myfunctions();

}
