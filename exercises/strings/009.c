#include <stdio.h>


//  Vamos contar a frequência de cada dígito em uma string.
int main(){
	
	char c; 
	int cont; 
	int result; 
	
	while((c = getchar()) != EOF)  { 
		if(c > '0' && c < '9'){ 
			result = c - '0'; 
			 
		}  
		
	}
	
	
}
